<p><%= t(:hello)+" "+@resource.email %>!</p>

<p><%= t('devise.unlock.has_been_locked') %></p>

<p><%= t('devise.unlock.click_link') %></p>

<p><%= link_to t('devise.unlock.account'), unlock_url(@resource, :unlock_token => @resource.unlock_token) %></p>

