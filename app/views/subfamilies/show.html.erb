<div class="row">
  <div class="col-md-12">
    <div class="box">
      <div class="box-header">
        <ul class="nav nav-tabs nav-tabs-left">
          <li class="active"><a href="#home" data-toggle="tab"><i class="icon-home"></i></a></li>
        </ul>
        <div class="title"><%= "MODEL" %></div>
      </div>

      <div class="box-content">
        <div class="tab-content">
          <div class="tab-pane active" id="home">
            <ul class="box-list">
                              <li>
                  <span><strong><%= t'.name' %></strong> <%= @subfamily.name %></span>
                </li>
                              <li>
                  <span><strong><%= t'.description' %></strong> <%= @subfamily.description %></span>
                </li>
                              <li>
                  <span><strong><%= t'.family' %></strong> <%= @subfamily.family %></span>
                </li>
                              <li>
                  <span><strong><%= t'.company_id' %></strong> <%= @subfamily.company.name %></span>
                </li>
                              <li>
                  <span><strong><%= t'.branch_id' %></strong> <%= @subfamily.branch.name %></span>
                </li>
                              <li>
                  <span><strong><%= t'.client_id' %></strong> <%= @subfamily.client %></span>
                </li>
                              <li>
                  <span><strong><%= t'.client__branch_id' %></strong> <%= @subfamily.client_branch %></span>
                </li>
                          </ul>
          </div>
        </div>
      </div>
      <div class="box-footer padded">
        <span class="pull-right">
          <%= render "layouts/action_links" %>
        </span>
      </div>
    </div>
  </div>
</div>
